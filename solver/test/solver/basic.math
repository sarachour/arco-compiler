name "a super simple example."

type pint

input X : pint
input Y : pint
output Z : pint
param PF : pint = 0.5

assume mag X in (-2,2) : pint
assume mag Y  in (-2,2) : pint
assume mag Z in (-20,20) : pint


rel Z = (X*Y)/PF + Y + 0.5
