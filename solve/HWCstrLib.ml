open HWData
open HWLib
open Common

module HwCstrLib = 
struct
  let mk_sample_cstr env name pname prop freq typ = ()
  let mk_repr_cstr env name pname prop sign exp mant = ()
  let mk_mag_cstr env name pname prop min max typ = ()

end
