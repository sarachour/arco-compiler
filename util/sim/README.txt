
# Component Files
Component files describe the behavior of the component.

	`.comp`: Component files, from which circuits are generated.
	`.ckt` circuit files, which are standalone spice definitions with concretized variables. 
	`.spec`: spec files, which describe the models.
	`.spice-params`: file outlining params in spice.
	`.model-params`: file outlining model params.
